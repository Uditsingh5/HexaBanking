# Copy this file to .env in this same server folder, then fill in real values.
# dotenv loads server/.env automatically (not the repo root).

PORT=3000
MONGO_URI="mongo uri"

JWT_SECRET=12345
CLIENT_ORIGIN=http://localhost:5173

# ── Email (pick ONE method) ──────────────────────────────────────────
# Option 1 (Recommended): Gmail App Password
#   → Enable 2FA on your Google account
#   → Generate at https://myaccount.google.com/apppasswords
EMAIL_USER=smtp_email
# EMAIL_APP_PASSWORD=your-16-char-app-password

# Option 2: Gmail OAuth2 (tokens can expire if app is in "Testing" mode)
# CLIENT_ID=smtp_client_id
# CLIENT_SECRET=smtp_client_secret
# REFRESH_TOKEN=smtp_refresh_token

